'use strict'

class Actions {

    createOrUpdate(scope, uuid, name){
        var promise = new Promise(function(resolve, reject){

        });
        return promise;
    }
}

module.exports = Actions;